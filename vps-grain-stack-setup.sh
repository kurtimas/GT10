#!/usr/bin/env bash
#===============================================================================
#  LEGACY — superseded. Do NOT run.
#
#  This was a generic variant of the GTv8-era VPS setup (XFCE/XRDP desktop,
#  hardening, Docker). It targets the old GTv8Beta stack and is kept only as a
#  pointer so old links don't 404.
#
#  Canonical installer: ./gtv8-vps-setup.sh  (repo root)
#  Operator guide:      ./Grain-Tracker-Install-Guide.md
#===============================================================================
echo "This script is LEGACY and no longer provisions the current stack." >&2
echo "Use ./gtv8-vps-setup.sh instead (see Grain-Tracker-Install-Guide.md)." >&2
exit 1
