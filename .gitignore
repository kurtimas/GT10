# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# local data (SQLite offline databases, runtime data)
data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# environment / secrets
.env
.env.*
!.env.example

# logs
logs/
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Agent session artifacts
.zcode/

# Historical zip archives — kept locally, never in the repo
# (they contain real operational data and a retired password)
GTv8Beta.zip
GTv8Beta-update.zip
grain-track-ubuntu-bootstrap.zip

# superdesign temp html
.superdesign/tmp/
